#!/bin/sh

mkdir -p "$1" "$2"
build=$(cd "$1/" && pwd)
cache=$(cd "$2/" && pwd)
env_dir="${3}"

BIN_DIR=$(cd $(dirname $0); pwd)
BUILDPACK_DIR=$(dirname $BIN_DIR)

. "$BIN_DIR/support/functions.sh" $1 $2 $3
. "$BIN_DIR/support/sqlite3.sh" $1 $2 $3
. "$BIN_DIR/support/libvips.sh" $1 $2 $3
